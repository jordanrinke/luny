purpose: This module implements the strip command that removes @toon comment blocks
tokens: ~4422
exports[1]: run_strip(fn)
signatures[1]:
  run_strip(fn): (args: &StripArgs, root: &Path, _verbose: bool) Result<()>
imports[10]{from,items}: crate::cli,StripArgs; crate::parser,ParserFactory; anyhow,Context|Result; std,fs; std::io,self|Read|Write; std::path,Path; super::*,super::*; std,fs; std::path,PathBuf; tempfile,TempDir
calls[4]{target,methods}: crate::parser,new; tempfile,new().unwrap|new; std::path,from; std,create_dir_all(&subdir).unwrap|write|write(&input_path, source).unwrap|write(&output_path, &stripped).context|read_to_string(&path).context|read_to_string(&output_path).unwrap|create_dir(&subdir).unwrap|create_dir|create_dir_all|write(subdir.join("test.ts"), source).unwrap|read_to_string
